# Quotes for the tmux status bar, one per line; blank lines and #-comments are
# ignored. Keep them ASCII and under 100 characters: quote.sh pads its field by
# bytes, so a smart quote or dash would silently shift the rest of the bar, and
# a long quote makes the typewriter cycle drag.
Make it work, make it right, make it fast.
Premature optimization is the root of all evil.
Simplicity is prerequisite for reliability.
Testing shows the presence, not the absence, of bugs.
Controlling complexity is the essence of programming.
Debugging is twice as hard as writing the code.
Programs must be written for people to read.
Talk is cheap. Show me the code.
First, solve the problem. Then, write the code.
The best code is no code at all.
Deleted code is debugged code.
Weeks of coding can save hours of planning.
Make the change easy, then make the easy change.
If it hurts, do it more often.
Given enough eyeballs, all bugs are shallow.
Adding people to a late project makes it later.
Plan to throw one away; you will anyway.
There is no silver bullet.
Do one thing, and do it well.
When in doubt, use brute force.
Code is read far more often than it is written.
Everything should be built top-down, except the first time.
Correctness is clearly the prime quality.
Any fool can write code a computer understands.
A little copying beats a little dependency.
Clear is better than clever.
Errors are values; handle them.
Don't guess at performance. Measure it.
The cheapest feature is the one you never build.
Fix the cause, not the symptom.
